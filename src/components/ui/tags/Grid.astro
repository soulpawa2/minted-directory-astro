---
import type Tag from "../../../types/Tag";

const tags = (await themeConfig).directory.tags as Tag[] | undefined;
// const { selectedTags, toggleTagByName } = useTags();
---

<div class="flex flex-wrap gap-2 mt-4">
  {
    tags?.map((tag) => (
      <span
        class="border rounded-md px-2 py-1 hover:bg-gray-50 dark:hover:bg-gray-900 dark:border-gray-600 cursor-pointer select-none"
        data-value={tag.name}
        id="toggle-tag"
      >
        {tag.key}
      </span>
    ))
  }
</div>

<script>
  // selectedTags.includes(tag.name)
  //           ? "border-primary-500 dark:border-primary-300"
  //           : ""
</script>
